---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.1
  name: thanosgateways.monitoring.thanos.io
spec:
  group: monitoring.thanos.io
  names:
    kind: ThanosGateway
    listKind: ThanosGatewayList
    plural: thanosgateways
    singular: thanosgateway
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ThanosGateway is the Schema for the thanosgateways API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ThanosGatewaySpec defines the desired state of ThanosGateway
            properties:
              headerManipulations:
                description: |-
                  HeaderManipulation defines the configuration for header manipulation.
                  Header manipulations happens at a global level and applies to all requests.
                properties:
                  fromHeader:
                    description: FromHeader is the header to copy the value from.
                    type: string
                  toHeader:
                    description: ToHeader is the header to copy the value to.
                    type: string
                required:
                - fromHeader
                - toHeader
                type: object
              log_level:
                default: info
                description: LogLevel is the log level for the server.
                type: string
              metricsReadSpec:
                description: MetricsReadSpec defines the configuration for reading
                  metrics.
                properties:
                  backendSpec:
                    description: BackendSpec defines the configuration for the backend.
                    properties:
                      backendConfig:
                        description: BackendConfig is the configuration for the backend
                          service.
                        properties:
                          address:
                            description: Address is the address of the backend.
                            type: string
                          port:
                            description: Port is the port of the backend.
                            format: int32
                            type: integer
                        required:
                        - address
                        - port
                        type: object
                      mtlsConfig:
                        description: MTLSConfig is the configuration for mTLS.
                        properties:
                          matchSANs:
                            description: |-
                              MatchSANs is the list of SANs to match.
                              If not specified, the SANs in the server certificate will not be checked.
                            items:
                              type: string
                            type: array
                          serverCert:
                            description: ServerCert is the path to the server certificate.
                            type: string
                          serverKey:
                            description: ServerKey is the path to the server key.
                            type: string
                          trustedCA:
                            description: TrustedCA is the path to the trusted CA certificate.
                            type: string
                        required:
                        - serverCert
                        - serverKey
                        - trustedCA
                        type: object
                      tokenAuthConfig:
                        description: TokenAuthConfig is the configuration for token
                          authentication.
                        properties:
                          enableKubernetesTokenReview:
                            default: false
                            description: |-
                              EnableTokenReview enables token review.
                              If not specified, token review will not be enabled.
                            type: boolean
                        type: object
                    required:
                    - backendConfig
                    type: object
                  policies:
                    description: Policies is a list of label based access control
                      policies to be applied to the request.
                    items:
                      description: |-
                        Policy is a list of CEL expressions and matchers.
                        If all the CELExpressions evaluate to true the Selectors are injected into the request.
                      properties:
                        expression:
                          description: CELExpression is a CEL expression that must
                            evaluate to true for the policy to be applied.
                          type: string
                        name:
                          description: Name is a human-readable name for the policy.
                          type: string
                        selectors:
                          description: Selectors is a list of matchers to be injected
                            into the request as part of the policy if the CELExpression
                            evaluates to true.
                          items:
                            description: Selector is a label selector that will be
                              applied if the ConditionalSelector evaluates to true.
                            properties:
                              conditional_selector:
                                description: |-
                                  ConditionalSelector is a list of selectors that must all evaluate to true for the Selector to be applied.
                                  This is optional and if not present the Selector will be applied if the CELExpression evaluates to true.
                                type: string
                              label_selector:
                                description: LabelSelector is the label selector that
                                  will be applied if all the selectors in the ConditionalSelector
                                  are true.
                                type: string
                            required:
                            - label_selector
                            type: object
                          type: array
                      required:
                      - expression
                      - name
                      - selectors
                      type: object
                    type: array
                  rbacConfig:
                    additionalProperties:
                      type: string
                    description: RBACConfig is the configuration for RBAC.
                    type: object
                required:
                - backendSpec
                type: object
              metricsWriteSpec:
                description: MetricsWriteSpec defines the configuration for writing
                  metrics.
                properties:
                  backendSpec:
                    description: BackendSpec defines the configuration for the backend.
                    properties:
                      backendConfig:
                        description: BackendConfig is the configuration for the backend
                          service.
                        properties:
                          address:
                            description: Address is the address of the backend.
                            type: string
                          port:
                            description: Port is the port of the backend.
                            format: int32
                            type: integer
                        required:
                        - address
                        - port
                        type: object
                      mtlsConfig:
                        description: MTLSConfig is the configuration for mTLS.
                        properties:
                          matchSANs:
                            description: |-
                              MatchSANs is the list of SANs to match.
                              If not specified, the SANs in the server certificate will not be checked.
                            items:
                              type: string
                            type: array
                          serverCert:
                            description: ServerCert is the path to the server certificate.
                            type: string
                          serverKey:
                            description: ServerKey is the path to the server key.
                            type: string
                          trustedCA:
                            description: TrustedCA is the path to the trusted CA certificate.
                            type: string
                        required:
                        - serverCert
                        - serverKey
                        - trustedCA
                        type: object
                      tokenAuthConfig:
                        description: TokenAuthConfig is the configuration for token
                          authentication.
                        properties:
                          enableKubernetesTokenReview:
                            default: false
                            description: |-
                              EnableTokenReview enables token review.
                              If not specified, token review will not be enabled.
                            type: boolean
                        type: object
                    required:
                    - backendConfig
                    type: object
                required:
                - backendSpec
                type: object
              replicas:
                default: 1
                description: Replicas is the number of proxy replicas.
                format: int32
                minimum: 1
                type: integer
            required:
            - metricsReadSpec
            - metricsWriteSpec
            - replicas
            type: object
          status:
            description: ThanosGatewayStatus defines the observed state of ThanosGateway
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
