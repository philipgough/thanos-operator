---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.1
  name: thanosgateways.monitoring.thanos.io
spec:
  group: monitoring.thanos.io
  names:
    kind: ThanosGateway
    listKind: ThanosGatewayList
    plural: thanosgateways
    singular: thanosgateway
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ThanosGateway is the Schema for the thanosgateways API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ThanosGatewaySpec defines the desired state of ThanosGateway
            properties:
              headerManipulations:
                description: |-
                  HeaderManipulation defines the configuration for header manipulation.
                  Header manipulations happens at a global level and applies to all requests.
                properties:
                  fromHeader:
                    description: FromHeader is the header to copy the value from.
                    type: string
                  toHeader:
                    description: ToHeader is the header to copy the value to.
                    type: string
                required:
                - fromHeader
                - toHeader
                type: object
              log_level:
                default: info
                description: LogLevel is the log level for the server.
                type: string
              metricsReadSpec:
                description: MetricsReadSpec defines the configuration for reading
                  metrics.
                properties:
                  backendConfig:
                    description: BackendConfig defines the configuration for the backend.
                    properties:
                      address:
                        description: Address is the address of the backend.
                        type: string
                      port:
                        description: Port is the port of the backend.
                        format: int32
                        type: integer
                    required:
                    - address
                    - port
                    type: object
                  headerModification:
                    description: HeaderModification defines the configuration for
                      header modification.
                    properties:
                      addHeaders:
                        additionalProperties:
                          type: string
                        description: AddHeaders is a list of headers to add to the
                          request.
                        type: object
                      removeHeaders:
                        description: RemoveHeaders is a list of headers to remove
                          from the request.
                        items:
                          type: string
                        type: array
                    type: object
                required:
                - backendConfig
                type: object
              metricsWriteSpec:
                description: MetricsWriteSpec defines the configuration for writing
                  metrics.
                properties:
                  backendConfig:
                    description: BackendConfig defines the configuration for the backend.
                    properties:
                      address:
                        description: Address is the address of the backend.
                        type: string
                      port:
                        description: Port is the port of the backend.
                        format: int32
                        type: integer
                    required:
                    - address
                    - port
                    type: object
                  headerModification:
                    description: HeaderModification defines the configuration for
                      header modification.
                    properties:
                      addHeaders:
                        additionalProperties:
                          type: string
                        description: AddHeaders is a list of headers to add to the
                          request.
                        type: object
                      removeHeaders:
                        description: RemoveHeaders is a list of headers to remove
                          from the request.
                        items:
                          type: string
                        type: array
                    type: object
                required:
                - backendConfig
                type: object
              replicas:
                default: 1
                description: Replicas is the number of proxy replicas.
                format: int32
                minimum: 1
                type: integer
            required:
            - metricsReadSpec
            - metricsWriteSpec
            - replicas
            type: object
          status:
            description: ThanosGatewayStatus defines the observed state of ThanosGateway
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
