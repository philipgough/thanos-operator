---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.1
  name: thanosgateways.monitoring.thanos.io
spec:
  group: monitoring.thanos.io
  names:
    kind: ThanosGateway
    listKind: ThanosGatewayList
    plural: thanosgateways
    singular: thanosgateway
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ThanosGateway is the Schema for the thanosgateways API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ThanosGatewaySpec defines the desired state of ThanosGateway
            properties:
              headerManipulations:
                description: |-
                  HeaderManipulations defines the configuration for header manipulation.
                  Header manipulations happens at a global level and applies to all requests.
                items:
                  description: |-
                    HeaderManipulation defines the configuration for header manipulation.
                    It enables copying of a header value at request time to another header.
                    This process runs before the request is sent to the backend and is matched against any requirements
                  properties:
                    fromHeader:
                      description: FromHeader is the header to copy the value from.
                      type: string
                    toHeader:
                      description: ToHeader is the header to copy the value to.
                      type: string
                  required:
                  - fromHeader
                  - toHeader
                  type: object
                type: array
              log_level:
                default: info
                description: LogLevel is the log level for the server.
                type: string
              replicas:
                default: 1
                description: Replicas is the number of proxy replicas.
                format: int32
                minimum: 1
                type: integer
            required:
            - replicas
            type: object
          status:
            description: ThanosGatewayStatus defines the observed state of ThanosGateway
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
